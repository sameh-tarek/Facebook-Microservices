server:
  port: 8000

spring:
  application:
    name: post-service
  datasource:
    url: "jdbc:mysql://localhost:3306/user_db"
    username: facebook
    password: facebook

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
